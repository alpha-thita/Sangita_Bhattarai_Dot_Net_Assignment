@model OnlineStoreFurniture.Models.Category

@{
    ViewData["Title"] = "Category Details";
}

<h1>@ViewData["Title"]</h1>

<div>
    <h2>@Model.Name</h2>
    <p>@Model.Description</p>
</div>
<a href="@Url.Action("Index", "Category")" class="btn btn-secondary">Back to List</a>


